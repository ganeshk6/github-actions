name: Second Workflow
on: push

jobs:
  hello-workflow:
    runs-on: ububuntu-latest
    steps:
      - name: Hello second workflow
        run: echo "Hello second workflow"
  bye-workflow:
    runs-on: ubuntu-latest
    steps:
      - name: Bye second workflow
        run: eco "Bye second workflow" 
  
